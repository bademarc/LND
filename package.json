{
  "name": "layeredge-network-defender",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer Online Strategy/Simulation Game based on LayerEdge technology.",
  "scripts": {
    "start:server": "cd server && npm install && npm start",
    "start:client": "echo \"Client is served via the Node.js server from the public directory. Run start:server and open http://localhost:3000\"",
    "dev:server": "cd server && npm install && npm run dev",
    "install:all": "npm install && cd server && npm install && cd .. && cd client && npm install",
    "dev": "npm run dev:server" 
  },
  "keywords": [
    "phaser",
    "nodejs",
    "websockets",
    "blockchain",
    "simulation",
    "strategy"
  ],
  "author": "LayerEdge Development Team (with AI Co-Pilot)",
  "license": "MIT",
  "workspaces": [
    "server",
    "client" 
  ]
}